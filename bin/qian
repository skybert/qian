#! /usr/bin/env bash

csv=$1

tag=skybert.net/qian
if [ -f "${csv}" ]; then
  d=$(dirname "${csv}")
  volumes="
    --volume ${d}:${d}
  "
fi

docker \
  run \
  -ti \
  ${volumes} \
  "${tag}" \
  "$@"
